configuration:
  dependencies:
    - mvn://ca.ibodrov.mica:mica-concord-task:0.0.5-SNAPSHOT

flows:
  default:
    - task: mica
      in:
        action: listEntities
        search: foo
      out: result
    - log: ${result.data}
