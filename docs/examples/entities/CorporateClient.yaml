name: CorporateClient
kind: /mica/kind/v1
schema:
  properties:
    apiVersion:
      enum: [ "client.example.com/v1" ]
    clients:
      type: array
      items:
        properties:
          id:
            type: string
          name:
            type: string
          validationUrl:
            type: string
    shared:
      type: any
